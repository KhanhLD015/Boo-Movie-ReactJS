export function SmoothHorizontalScrolling(e, time, amount, start) {
  var eAmt = amount / 100;
  var curTime = 0;
  var scrollCounter = 0;
  const Y = window.scrollY;
  return <div>SmoothHorizontalScrolling</div>;
}
